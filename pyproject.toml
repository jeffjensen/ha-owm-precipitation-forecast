[tool.ruff]
select = ["E", "F", "B"]
line-length = 100

[tool.mypy]
python_version = "3.12"
strict = true

[tool.coverage.run]
branch = true
source = ["custom_components"]

[tool.coverage.report]
fail_under = 80
